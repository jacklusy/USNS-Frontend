NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api
NEXT_PUBLIC_MOCK_MODE=true

# Mock auth demo accounts (MOCK_MODE=true):
# president@usns.edu / Password1!
# admin@usns.edu / Password1!
# dean@usns.edu / Password1!
# dba@usns.edu / Password1!
# faculty@usns.edu / Password1!
# Session expired access token (devtools): mock_access_session_expired
# Expired refresh token: mock_refresh_session_expired
# Reset password demo token: valid_reset_token
# Expired token: expired_reset_token
